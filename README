chliu's dotfiles

(forked from Ryan Bates)

These are config files to set up a system the way I like it.

Installation

git clone git://github.com/chliu/dotfiles ~/.dotfiles

cd ~/.dotfiles

rake install

#install vim plugins
ruby vim/update_bundles
